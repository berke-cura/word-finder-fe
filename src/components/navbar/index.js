import * as Styled from './style'

const NavBar =  () => {

    return (

        <Styled.Bar>

            <Styled.MenuItem>TEXT</Styled.MenuItem>
               
            <Styled.MenuItem>TEXT</Styled.MenuItem>
            
            <Styled.MenuItem>TEXT</Styled.MenuItem>

        </Styled.Bar>
        
    )
}
export default NavBar