import * as Styled from './style'
import { Contact } from '../../components'



const COsectionContainer =  () => {

    return (
        <Styled.Background>
            <Styled.Box>
                <Styled.Title>Contact</Styled.Title>
                <Contact/>
            </Styled.Box>
        </Styled.Background>

    )
}

export default COsectionContainer