import * as Styled from './style'

const Title =  () => {

    return (
       <Styled.TitleBox>
           <Styled.Title>WORD FINDER</Styled.Title>
           </Styled.TitleBox>    
    )
}
export default Title